<h1>Userlist List</h1>
<div class="Users">
  {{#each users}}
    <h2>{{username}}</h2>
    {{#if isAdmin}}
    <span>
    <a href="{{_id}}">Delete User</a>
    </span>
    {{/if}}
  {{/each}}
  <br><br>
  <a href="/auth/logout">Logout</a>
</div>